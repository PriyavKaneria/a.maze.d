// place files you want to import through the `$lib` alias in this folder.
export type Entry = {
    name: string;
    link: string | null;
    time: number;
    items: number;
    hardmode: boolean;
};